({
	helperMethod : function() {
		
	},
	setSlideWidth: function (component) {
	    console.log('slideWidth:' + component.find("gallery"));
        //var slideWidth = component.find("gallery").getElement().offsetWidth;
        //component.set("v.slideWidth", slideWidth);
    }
})